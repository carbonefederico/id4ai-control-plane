# Copy to .env and fill in your values
PORT=3033

# GitHub repo containing PingGateway route files
GITHUB_OWNER=<your-github-org-or-user>
GITHUB_REPO=<your-gateway-config-repo>
GITHUB_PATH=config/routes
GITHUB_REF=main
# GITHUB_TOKEN=<your-github-pat>   # required for private repos

# PingGateway — public base URL prepended to each route's condition path
GATEWAY_PUBLIC_URL=http://localhost:8080

# Advanced Identity Cloud (AIC) — service account for agent inventory
AIC_HOST=<your-tenant>.forgeblocks.com
AIC_SERVICE_ACCOUNT_ID=<your-service-account-uuid>
AIC_JWK_PATH=./privateKey.jwk
AIC_REALM=/realms/root/realms/alpha
AIC_SCOPES=fr:am:* fr:idm:*

# PingAuthorize
PAZ_BASE_URL=https://localhost:9443
PAZ_BRANCH=<your-paz-branch-uuid>
PAZ_USER_ID=admin
